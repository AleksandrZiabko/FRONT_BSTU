import axios from 'axios';

const API_URL = 'http://localhost:3777';

export const uploadForm = async (obj, url) => {
    return new Promise(resolve => {
        axios.post(API_URL + url, obj, {
            'responseType': 'json',
        }).then(res => {
            resolve (res.data);
        });
    })
};